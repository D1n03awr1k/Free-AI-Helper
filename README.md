# Telegram Bot with Hugging Face API

This is a Telegram bot that uses the Hugging Face API (e.g., GPT-2 model) to generate responses to user messages.

## How to Run

1. **Install dependencies**:
   ```bash
   pip install -r requirements.txt
